# 真机 / 人工验证清单（2026-09-06）

> 配套：`doc/references/audit-fix-log-2026-09.md`（本轮代码修复）、`doc/references/project-audit-report-2026-09.md`（审计）。
> 以下事项**无法在模拟器/静态检查中闭环**，需按序人工验证。建议一台 iPhone（iOS 26+）+ 一台局域网 1Panel 面板。
> 勾选方式：把 `[ ]` 改 `[x]` 并补充一行「结果：…」。

---

## A. 立即要做的两项（安全/功能闸门）

- [x] **E8-1 面板侧轮换 API 密钥**（ADR-0001 唯一遗留）：
      1Panel 面板 → 面板设置 → API 接口 → 重新生成密钥；随后用旧密钥请求一次应被拒绝。
      ⚠️ 轮换前，仓库历史中的旧密钥仍可操作测试面板。
      结果：面板为测试面板无真实业务数据、仅局域网内可以访问，不对外开放、后续测试完成后会直接移除整台测试机
- [x] **C1 真机本地网络授权（原 R1）**：
      1) 删除 App 重装 → 添加局域网面板（如 `http://10.0.0.1:36130`）→ 测试连接，
         首次请求应弹「"1PanelClient"想查找并连接到本地网络上的设备」；
      2) 允许后连接成功；
      3) 拒绝一次 → 重装/重试，应表现为超时 + 新错误文案（含「设置 → 隐私与安全性 → 本地网络」指引）；
      4) 到该设置项手动开启后，下拉刷新可恢复连接。
      结果：正常

## B. 轮询与性能（D1/D3/D4 修复的运行时验证）

- [x] **D1-1 后台零轮询**：App 退后台挂 10 分钟，期间 dashboard/current 请求数应为 **0**
      （Charles/代理抓包，或 Console 过滤 `1panel`；修复前静态推断 ≈ 每台服务器 120 次）。
      结果：通过网关看请求、面板-主机日志发现发往测试机(192.168.50.20)的请求为0
- [x] **D1-2 回前台补拉**：后台 ≥6 秒后回前台，首页状态卡应立即刷新一次（不等下一个 5 秒）。
      结果：正常
- [x] **D1-3 错峰与退避**：添加 ≥3 台服务器进入服务器页，抓包看 dashboard/current 是否按 ~250ms 间隔错峰发出；
      关掉其中一台的面板，观察其请求频率下降（1→2→4 轮跳过，最稀约 20 秒一次）；下拉刷新应立即重试它。
      结果：只有一台测试服务器，将这台测试服务器添加3次，通过面板-主机日志查看dashboard/current请求确有秒数的差别，其中两个在43秒时，另外1个在44秒时
- [x] **D3 去重**：首页 → 推入服务器页停留 30 秒，抓包确认**当前服务器**的 dashboard/current
      每周期只有 1 次（修复前首页+服务器页双发）；返回首页后首页轮询恢复且立即补拉。
      结果：进入服务器页通过面板-主机日志查看dashboard/current请求每5秒内只有一个请求，返回后会刷新首页信息
- [x] **A1 冷启动基线**：Instruments App Launch 模板，冷启动→首帧时间记录入报告 §9 基线表。
      结果：没有专业的同步点击计时工具，手动人眼大致判断，大概00:00.50左右
- [x] **A2 重页面 hitch（含 D4 效果）**：Instruments Hitches，走查 数据库（pageSize 200）、
      日志（500）大列表滚动，记录 hitch 数作基线（D4 已把解码移出主线程，可对比修复前主观体感）。
      方法：真机 + Xcode 开发签名安装（同 A3/D2 前提；设备避免发烫、亮度固定）→
      Product → Profile（⌘I）→ Hitches 模板 → 每个重列表固定动作：等首屏加载完 → 慢速滚到底 →
      快速甩动 3-4 次 → 触发加载更多（约 30s/列表；跑 2 遍取第 2 遍——首滚有 SwiftUI 着色器
      预热 hitch）。每列表记五项：滚动时长 / hitch 总数 / **Hitch Time Ratio**（<5ms/s 绿、
      5-30 黄、>30 红）/ 最大单次 hitch / hitch 集中位置（首屏解码 or 加载更多一拍 or 持续滚动）。
      靶列表：防火墙 215 条（滚到全量）、操作日志 509 条（翻页到全量）、系统日志 500 行、数据库。
      结果：防火墙 58.87s / 16 次 / hitch 时长 8.34→16.67ms（ratio 推算 ≈3.3ms/s，绿）/
      集中于持续滚动（最差掉 1 帧）；操作日志 71.47s / 23 次 / hitch 时长 8.34→83.35ms
      （ratio 推算 ≈3.9ms/s，绿）/ 83ms 尖峰（5 帧）发生在加载更多一拍，解码+插入 100 行
      所致，属预期（将来优化方向=分批插入）；两列表均处流畅区间（<5ms/s）。
      备注：系统日志/数据库列表未测（下次有数据顺手补）；ratio 为按 hitch 次数×时长
      推算值，Instruments 汇总面板的 Hitch Time Ratio 字段可直接读
- [x] **A3/D2 内存基线**：14 页遍历后内存 + 监控页挂 30 分钟内存曲线（Leaks/Allocations）。
      方法：真机（模拟器无 jetsam 压力、曲线失真）→ **须用 Xcode 开发签名安装**
      （Product → Profile/⌘I，Release 配置更接近真实；自签工具/LiveContainer 装的包无
      get-task-allow，Instruments 会报 permission to debug denied，先删掉旧副本再装）→
      Xcode → Open Developer Tool → Instruments → Allocations 模板 → 选设备与
      1PanelClient → Record：①冷启动空闲 30s 记基线；②按序遍历
      14 个快照页、每页停 5-10s，看「All Heap & Anonymous VM」曲线的峰值与回落后平台；
      ③停在监控页 30 分钟（先把自动锁定改为永不），看曲线应为锯齿稳定而非单调爬升。
      健康形态=阶梯上升后回落、监控页水平锯齿；病理形态=只升不降（retain cycle/无上限缓存）、
      Leaks 模板出现红色标记。记录三个口径：冷启动空闲 / 遍历后 / 监控 30min 起点-终点-峰值。
      快查替代：Xcode 调试时底部内存仪表看趋势、Debug Memory Graph 查持有环；
      正常使用一天后「设置 → 关于 → 诊断数据（本地）」的 MetricKit 日报含内存指标可作旁证。
      结果：1、冷启动空闲 25.62MiB；2、14 页遍历：有起伏但整体上涨（首轮属正常——页面 VM
      缓存与解码缓存驻留；判定标准是**第二轮遍历**不再显著上涨）；3、监控 30min：起点 62.23 /
      峰值 68.65 / 终点 57.56MiB——终点低于起点，锯齿稳定，**无泄漏**。
      重列表滚动内存（滚动中抬升约 8MiB 后回落，健康）：防火墙 40.51→48.71→35.22MiB、
      操作日志 41.87→50.58→38.29MiB
- [x] **A5 包体正式口径**：Xcode Organizer → 生成 App Thinning 报告，记录 App Store 估算大小
      （初基线：单架构未压缩 17MB，见报告 §2.4）。
      结果：无 Organizer 权限，改用本地等价口径（2026-09-07 实测）：`./build-ipa.sh` 以
      generic/iOS（arm64 单架构，即 ASC 交付架构）出未签名包——IPA 压缩体积 **9.1MB**
      ≈ App Store 下载大小；解压后 .app **17MB** ≈ 安装大小（主二进制 13MB + Assets.car 4MB），
      与审计初基线一致。待有权限后在 Organizer 复核官方 App Thinning 报告，差异预期极小

## C. UI / 本地化走查

- [x] **B8 en 模式快捷指令与 Widget**：系统语言切 English →
      1) Shortcuts App 搜 1Panel：五个意图的标题/描述/参数（服务器/容器/网站/计划任务）应全为英文；
      2) 容器操作/网站启停的枚举值显示 Start/Stop/Restart 等；
      3) Widget 商店（长按桌面 → + → 搜 1Panel）：两个 Widget 的名称与描述为英文。
      结果：系统中文，APP英文，1、快捷指令搜索1PanelClient显示: 服务器概览、容器操作、检查服务器状态、网站起停、执行计划任务；2、对xx进行 启动/停止/重启/关闭 操作；3、2个小组件为: 服务器状态、容器操作
- [x] **E6 权限弹窗英文**：系统语言 English → 设置内开启应用锁 → 触发 FaceID：
      系统弹窗用途文案应为英文（"Used to unlock the app…"）；系统设置的 App 权限页同理。
      （注意：此弹窗跟随**系统语言**，不跟随应用内语言切换，属预期。）
      结果：确实显示英文，人脸认证显示: Authenticate to continue
- [x] **B1 替换符号视觉走查**：自签证书入口/CA 列表（checkmark.seal）、终端快捷命令空态（terminal）、
      WAF 三处空态（shield/globe/number）、应用升级无版本（arrow.up.circle）、网站日志追踪开关（waveform）、
      重定向（arrow.uturn.right）——确认图标可见且语义可接受。
      结果：还行，可以接受
- [x] **B4 走查矩阵**：深色/浅色 × 动态字号最大档 × 横屏/iPad 分屏，过一遍主要页面（静态已确认零固定前景色）。
      结果：正常
- [x] **B5 Accessibility Inspector**：对比度、44pt 命中区、Reduce Motion（重点：AppLock/MonitorView 固定字号页）。
      方法：Xcode → Open Developer Tool → Accessibility Inspector，左上角目标选运行的
      模拟器/真机（对比度与命中区用模拟器即可，Reduce Motion 建议真机）——
      ① **Audit 标签 → Run Audit**：逐页跑自动审计，记录 Contrast（文本对比 <4.5:1）、
      Hit Region（可交互元素 <44×44pt）、Dynamic Type（固定字号文本）、Text Clipping
      四类的 issue 数与具体元素；重点页：锁屏键盘、监控页图表、防火墙/日志行内小按钮、
      端口白名单编辑/删除按钮；
      ② **Inspect 标签（十字准星）**：抽查纯图标按钮（切换服务器、WAF 火焰图标、终端
      三个点菜单、分享导出）是否都有 accessibilityLabel；
      ③ **Reduce Motion**：真机 设置 → 辅助功能 → 动态效果 → 减弱动态效果 打开后走查——
      Tab 栏滑入/侧栏收展应退化为淡入淡出（工程已接 accessibilityReduceMotion），
      记录仍会动的动画及是否可接受。
      结果：五页 Audit 分诊（报告存 `logs/Audit/*.html`）——
      ① Contrast（首页32/监控36/防火墙44/锁屏60/终端13）：绝大多数为材质混色取样误报
      （fg≈bg 近白对，锁屏 60 条全是）与 .secondary/.tertiary 系统灰权衡（Apple secondaryLabel
      同款水平），均无需修；
      ② Dynamic Type（42/73/63/54/29）：真固定仅首页 RingStatView 百分比 1 类（B2 漏网，已修
      panelScaled）；其余为固定高度容器（圆环/Tab 行/键盘键位，有意设计）与 panelScaled 被
      审计器不识别，B4 最大档人工走查正常佐证；
      ③ Hit Region 5 条真问题已修：监控页三个图表展开按钮 15×9→44×44、防火墙状态卡展开
      箭头 11×7→44×44、锁屏底部切换按钮 18pt 高→44；
      ④ Element Description 4 条已处置：锁屏顶部生物识别图标改 accessibilityHidden（下方文本
      已表意）、删除键图片层补「删除」标签、终端 SwiftTerm 画布补「终端」标签（内容读屏
      不友好属第三方库固有限制）、防火墙 1 条为版本号文本（v2.x，审计器启发式，权衡保留）；
      Clipped Text 1 条（端口规则段头）为大字号下截断预警，权衡保留；
      ⑤ 图标按钮 label 抽查正常；Reduce Motion 真机走查体感可接受。
      修复明细见 `audit-fix-log-2026-09.md` 第四轮补充
- [x] **E7 版本显示**：设置 → 关于：版本应显示 `0.1.14`（现唯一来源 MARKETING_VERSION；不再有源码双写）。
      结果：正常

## D. 数据量与功能边界

- [x] **C5 大数据量截断**：网站列表 >20 条（pageSize=20）、防火墙规则 >200、日志 >500 条的真机表现
      （是否需要上拉加载/提示被截断）。
      结果：网站22条正常、防火墙规则215条，只显示200条、操作日志509条，只显示100条。上拉不加载
      → 2026-09-07 第三轮已补分页（见 `audit-fix-log-2026-09.md` C5 行）：防火墙三段 + 操作/访问日志
      均支持滚动到底自动追加；复验：防火墙 215 条应显示全量（段头显示总数）、操作日志滚到底可继续加载
- [x] **F2 主流程回归**：添加服务器 → 首页/管理各模块浏览 → 容器启停/网站启停 → 终端 → Widget 刷新 →
      移除服务器（确认 Keychain 清理）。
      ⚠️ 2026-09-07 直装真机发现（均已在第三轮修复，见 `audit-fix-log-2026-09.md`）：
      ① 添加服务器后小组件一直「离线：请先添加服务器」——主 App 从不调 WidgetCenter 重载（W1），
        复验：添加/删除/切换服务器后小组件数秒内重载；重装 App 后打开一次即恢复；
      ② 取消人脸认证无限重弹（L1），复验：取消一次后不重弹、点「解锁」才再弹；
      ③ 首页底部栏丢失（W2），复验：首页→服务器页→back 反复多次（原 1/3 概率丢）、
        首页→版本日志→back、设置→关于→back、三 Tab 交叉切换，底部栏均应回归；
      ④ 熄屏自动唤醒（L2），复验：App 前台放着不动等自动锁屏——手机应安静待机不弹
        FaceID；再解锁手机，FaceID 应在解锁后立刻出现（对齐其它 App 时序）。
      结果（2026-09-07 第一轮，部分通过）：②取消循环通过；④熄屏不唤醒通过；「解锁后自动弹」
      L2 回归已修，二轮复验通过（可接受）；①小组件：三轮复验（Xcode 直装 + 添加服务器 +
      桌面小组件 + 等 30s）仍离线 → 定位为扩展进程缺 ATS 豁免（W3，`http://` 面板地址在
      小组件进程被 ATS 全部掐断），已修 `PanelWidgets/Info.plist`，待四轮复验：重装 →
      添加服务器 → 小组件应在打开 App 后数秒到十几秒转在线并显示 CPU/内存；
      ③底部栏通过。另记：版本更新日志页加载期间返回按钮短暂居中、加载完成自恢复——
      iOS 26 从隐藏导航栏的根页推送的过渡观感问题，自愈型，P3 观察。1、小组件正常显示但是感觉不会刷新；2、正常，但是还是存在一个疑问: APP在前台但是无操作休眠锁屏了，他会自动弹人脸识别唤醒手机，这种不是应该不允许的才对吗；3、目前正常
  ——第四轮备注结论（2026-09-07 复核代码后补记）：**2 的疑问属设计时序、无需修改**——
  L2 修复禁止的是「熄屏瞬间在系统锁屏之上弹认证并唤醒屏幕」；而设备解锁、App 仍在前台时
  立即弹自己的认证，与银行类 App 行为一致，属正确时序。1 的「感觉不会刷新」属 WidgetKit
  系统重载预算所限（时间线 30 分钟 + 系统自行裁量；打开 App 即触发重载、数秒出新数据），
  为预期行为；曾评估「小组件显示数据更新时间戳」可改善观感，**决定暂不做**。
- [x] **E5-3 隐私政策本地页走查**：设置 → 关于 → 隐私政策：应为 App 内本地页（非跳网页），
      中英文各过一遍（切换应用内语言立即生效），页底「查看在线版本」链接可用。
      结果：正常（本地页/双语切换/诊断数据入口均通过）；「查看在线版本」404 已定位——
      链接原指向占位仓库 `a412316/1panel`，实际仓库为 `xy2026yi/1PanelClient`（公开），
      且对外文档目录是 git 跟踪的 `doc/`（`docs/` 被 gitignore，仅本地工作文档），链接
      已改指 `blob/main/doc/privacy-policy.md`、Issue 链接同步修正；`doc/privacy-policy.md`
      为新入库文件，**推送后在线链接生效**，装新构建复验一次即可

## E. 发布链路（提审前）

- [ ] **E1 隐私清单过审校验**：构建上传后 App Store Connect「隐私」页无警告；TestFlight 构建可正常安装。
      结果：
- [ ] **E4 出口合规**：ASC 活动信息里加密问答应已由 `ITSAppUsesNonExemptEncryption=false` 自动作答（不再弹问）。
      结果：

---

### 未决事项（2026-09-07 真机第四轮后更新）

> 第一轮发现的三个缺陷（Widget 不刷新 / FaceID 取消循环 / 首页底部栏丢失）均已修复，其中
> 前两项 + 底部栏已于第四轮复验通过（FaceID 时序疑问已答复，见 F2 第四轮备注结论）；
> A/C 节勾选项全部闭环。
> 上架口径不变：**暂不提审、只做上架准备**；提审链路项做出上架决定后再启动。

**代码侧待办：**
- ~~C4 逐页两阶段核对~~（2026-09-07 第四轮完成：全部达标；数据库系统详情页 async let 与
  直接 await 混用导致所有请求双发，已改单次全并行，见 `audit-fix-log-2026-09.md` 第四轮）。
- C5 分页剩余面（优先级低）：文件管理器目录 >200、WAF 各列表 100、AppIntents 参数 200——
  数据量未触顶暂不痛；网站/容器/应用/数据库四个主列表已于第四轮补齐（同款滚动追加模式）。

**真机/Instruments 待办（checklist 内未勾选项）：**
- A2 重页面 hitch 基线剩余列表（系统日志/数据库，有数据时顺手补，**当前暂缓**）。
  A3/D2 内存基线已于 2026-09-07 记录，数字健康（监控 30min 终点低于起点、
  重列表滚动后回落，无泄漏迹象）。
- ~~F2 主流程回归 + E5-3 隐私政策本地页走查~~（第四轮完成并勾选：Widget / FaceID /
  底部栏复验通过、FaceID 时序疑问已答复；E5-3 在线链接已随 `doc/privacy-policy.md`
  推送生效，新构建装机后顺手点一次「查看在线版本」即彻底闭环）。

> B4/B5/E7 已于 2026-09-07 完成勾选；B5 的无障碍修复（命中区/标签/字号）随本日构建生效，
> 复验时顺手点一遍监控页图表展开、防火墙状态卡展开、锁屏底部切换即可。

**已用本地口径闭合（原被阻塞）：**
- A5 包体：无 Organizer 权限，已用 `build-ipa.sh` 本地实测替代（IPA 9.1MB ≈ 下载大小、
  .app 17MB ≈ 安装大小，见 A5 结果行）；有权限后再回 Organizer 复核官方数字即可。

**上架决定后启动（口径未变）：**
- E1 ASC 隐私页/TestFlight 验证、E4 出口合规生效确认、E9 提审自查清单、E5 中国区备案（若上架且目标含中国区应最先启动，流程数周）。
